server {
    listen 8080;

    index index.html;
    root /usr/src/app;

    location / {
        try_files $uri /index.html;
    }

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}
